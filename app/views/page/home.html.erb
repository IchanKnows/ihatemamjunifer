<!DOCTYPE html>
<html>
<head>
	<title>I Really Really Hate M'am Junifer!</title>
</head>
<body>
<%= render 'page/topbar' %>
<div  align="center" class="container">
	<% @hate.each do |hate| %>
		<div class="well col-xs-4 col-xs-offset-4">
			<%= hate.name %> says: <br>
			<q><%= hate.message %> </q>
		<div class="col-sm-12">
			<%= link_to "delete message", hate_path(hate), method: :delete, data:{confirm:"Do you want to delete this message?"}, class: "btn btn-danger btn-xs" %>	
		</div>					
		</div>
		
	<% end %>
	<div class="col-sm-12">
		<%= link_to "write a hate message", new_hate_path, class: "btn btn-primary btn-lg" %>
	</div>
</div>
	
	


</body>
</html>